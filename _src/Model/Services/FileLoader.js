class FileLoader{
	/* Javascript file loader.
	
	Uses...
	> JSON Stringify
	
	This class supplies a static method to other modules
	which takes a filepath (should be supplied from the
	ParseMagic Component) and loads the content of the file.
	
	
	*/
	static getLoadables(){
		return true;
	}
	
}